import styles from './UserList.module.css'import Wrapper from "../UI/Wrapper";export default function UserList(props) {	const {users_arr, removeUser} = props;	const displayList = () => {		if (users_arr && users_arr.length === 0) {			return <h2 className={styles.no_users}>No Users</h2>		}		return users_arr.map(el =>			<li				className={styles.list_item}				key={el.id}				id={el.id}				onClick={e => removeUser(e.target.id)}			>				<h3 className={styles.list_item__name}>{el.name}</h3>				<h4 className={styles.list_item__age}>{el.age} age old</h4>			</li>		)	}	return (		<Wrapper type='light'>			<ul>				{displayList()}			</ul>		</Wrapper>	)}